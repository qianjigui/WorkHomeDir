#!/bin/bash

ADB=adb

if [ $# -eq 1 ];
then
    ADB=$1
fi

FILE=new_logcat.`date +%Y%m%d%H%M%S`.log

for i in `ls new_logcat*.log`
do
    mv $i old_$i
done

$ADB logcat -v long -b radio -b events -b system -b main | tee $FILE
